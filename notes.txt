TO-DO
===================

OK Break up buildBodies; fix reuse in particles mode
OK geos+materials => bodies
OK Multiple, reactive times
OK Remove cycles; time-based animations
OK dithering shader
OK video background
OK shards wireframe
OK full hedron: solid, WF
-- live control mechanism
-- regular particles


Reactivity:
-- hide/show elements (on beat) -> edges, shards
-- cut to objects (eg when showing shards on their own)
-- separately scale shards (abs level)
-- displace on peaks, fall back to center (on beat)
-- shake l/r
-- jump away from screen (when 2 hedrons watching movie)
-- BG movie intensity / color

